# Calls the central reusable workflow in the governance repo. This file may live
# in service repos, but it cannot weaken the scan: the logic is centralized in
# zyte-trials/governance and the org ruleset requires the "secrets / detect-secrets"
# check to pass. Renaming the job here only makes the required check go missing,
# which still blocks the PR.
name: secrets

on:
  pull_request: {}
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  # Job id "secrets" + reusable job "detect-secrets" => check context
  # "secrets / detect-secrets" (this is what Ruleset B requires).
  secrets:
    uses: zyte-trials/governance/.github/workflows/detect-secrets.yml@main
